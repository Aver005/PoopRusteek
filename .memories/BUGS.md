# BUGS
> Known defects, sorted by impact. Update on discovery/fix.
> Last updated: 2026-08-26 (**five-agent review of the day's context-compaction work**, **42** findings filed below under the prefix `compaction-review-2026-08-26 #N`. Now: **17 closed the same day** (the five data-loss fixes, then **#1, #2, #3, #16, #17**, then the evening batch **#4, #5, #6, #7, #8, #11, #26** — see the RESOLVED entries), **24 open** (#9, #10, #12-#15, #18-#25, #27-#36, spread HIGH 2 / MEDIUM 15 / LOW 7) and **1 accepted** (#37, `chars / 3`, owner decision). **#25 is two thirds closed** and stays open with its scope narrowed to the one third that remains (a verbatim `<template>` echo still validates). The evening batch closed the measurement-and-summary group: the context window is re-polled on every provider/model switch and goes to *unknown* — ladder off — the moment one starts, an old `[agent] auto_compact = false` is migrated instead of silently reversed, the prior summary reaches the summariser once instead of twice, mode 2 folds its chunk summaries into one form mechanically and gives each chunk its own upstream session, the harness driver applies `ToolOutputCleared` the way the TUI does, and mode 1 no longer drops what sits ahead of the first user message. Before, same day: the harness group is closed: the ladder is now reachable from a scenario (`[context]` table + provider-window discovery in the driver), a scenario can assert a rung actually fired (`[[expect.trace]]`), a run that produced nothing to judge is a failure, and the reserve no longer switches the ladder off on windows ≤ 20k. First scenario that exercises rung 1 end to end: `sandbox/scenarios/mock/rung-one-clears-tool-output.toml` (2/2; fails with exit 4 and `0 'context.prune' record(s)` once the window is taken away). `.docs/context-compaction.md` corrected in the same pass and given a «Известные расхождения» section. Before, same day: **context compaction steps 5-6**: rung 3 + `/compact` shipped, closing findings **#3** and **#4**. Tally re-counted against the entries below, because the two header lines had drifted apart: of the review's 20 findings, **9 are closed** (1, 2, 3, 4, 5, 11, 12, 15, 16), **1 refuted** (17), **10 open** (6, 7, 8, 9, 10, 13, 14, 18, 19, 20). Two new HIGH entries opened by the same work, both found by reading rather than by running: `/compact` is a no-op on DeepSeek and pollutes its server-side branch, and a stale doc comment in `src/context/summary.rs`. Before, same day: full-codebase review `.docs/review-2026-08-26-rust.md`, 20 findings — same-day fix batch closed 6 (1,2,5,11,12,16), see RESOLVED and `JOURNAL/2026-08-26.md`; the other 14 added below by severity, source of record is the review doc. Before, same day: the harness's first finding — UTF-16 tool output decoded as UTF-8 — found and fixed; see RESOLVED and `reference/HARNESS.md` §10. Before: 2026-07-15 quality audit +8 entries, same-day defect batch fixed 7 — see RESOLVED; full audit: `reference/AUDIT-2026-07-15-QUALITY.md`)
> Full audit digests: `reference/AUDIT-2026-07-02.md` (defects), `reference/AUDIT-2026-07-15-QUALITY.md` (quality/structure), `.docs/review-2026-08-26-rust.md` (2026-08-26 full-codebase review, 20 numbered findings — 9 closed, 1 refuted, 10 open as of the evening of 2026-08-26; referenced by number below)

## RESOLVED 2026-08-27

- ✅ **Суб-агент отдавал пустоту и на исчерпании шагов.** Вторая дверь того же дефекта: хвост `run_sub_agent` возвращал «последнее сообщение ассистента», а шаг с одним `<tool_use>` кладёт именно пустое сообщение — родитель получал `Ok("")` с `is_error = false`. Теперь берётся последний **непустой** текст, а если его нет — ошибка. Найдено ревью по лучшим практикам, 2 теста. `→ src/agent/sub_agent.rs`
- ✅ **Окно списка не расширялось вниз после увеличения терминала.** `ListWindow::new` зажимал начало окна по `len - 1`, а не по `len - visible`: сохранённое `ListCursor.scroll` приезжало от узкого окна, и модалка вопроса на растянутом экране показывала три строки из десяти и пустоту под ними. `→ src/app/list.rs`, 1 тест
- ✅ **Суб-агент отдавал пустой ответ как успешный.** `run_sub_agent` возвращал `Ok(visible)` даже при пустом `visible` — тот же дефект, что харнесс нашёл для главного цикла 2026-08-25 и где его закрыли; суб-агент защиту не унаследовал. Найдено ревью фазы 3 по неиспользованному `RetryBudget::take_empty`. `→ src/agent/sub_agent.rs`, 2 теста

- ✅ **DeepSeek сообщал симптом вместо причины при протухшем токене.** Создание сессии отвечает HTTP 200 с ошибкой в теле (`{"code":40003,"msg":"Authorization Failed (invalid token)"}`), поэтому проверка статуса её пропускала, а пользователь видел `Invalid session payload: missing chat_session.id`. Тот же класс, что и rate-limit внутри стрима. `session_create_error` достаёт `code`/`msg` из конверта. `→ src/provider/deepseek/session.rs`
- ✅ **Строгий гейт clippy был красным на `develop`** — два протухших `#[expect(dead_code)]` роняли `clippy -D warnings`, значит блокирующий `lint` в CI падал и dev-build не публиковался. `→ src/provider/types.rs`, `src/provider/deepseek/endpoints.rs`
- ✅ **Список MCP не прокручивался за курсором**, `McpViewState.scroll_offset` служил двум разным целям, а у панели провайдеров прокрутки не было вовсе — всё три закрыты фазой 1. См. `JOURNAL/2026-08-27.md`

## CRITICAL

- (none open — **#3 closed 2026-08-26**, see RESOLVED)

## HIGH


### Announcing work ends the turn as if it were done

A turn whose final reply is plain text and no tool call is treated as a
finished answer. The model frequently uses that shape to *announce* the next
step instead of answering: given an empty directory and "create three files",
a live run did `ls -la`, replied "Директория пуста, только .gitkeep. Создам три
файла." and stopped. The harness scored it `completed` with an empty workspace;
a user would see a confident message and no files.

Reproduce: `sandbox/scenarios/dev/greenfield-todo-page.toml`, which fails on
the file expectations rather than on the answer text.

Not obviously a code bug — the loop cannot read intent — so the first thing to
measure is whether the system prompt can remove the behaviour ("never end a
turn by announcing work you have not done"). A loop-side nudge on
announcement-shaped endings is the fallback, and needs the harness to show it
does not cost anything elsewhere.

- `[BUG]` review-2026-08-26 #6: `MCPManager` lock held across network I/O on admin paths (`keys/dispatch.rs`, `keys/mcp.rs` reload/toggle/reconnect/remove) — the same class already fixed for tool dispatch (`dispatch_generic_tool`), but the admin paths never got the short-lock shape. `settle_frame` polls the same mutex every frame, so `View::Mcp` freezes for the duration. `→ src/app/keys/mcp.rs`, `src/app/keys/dispatch.rs`
- `[BUG]` review-2026-08-26 #7: background agents (`/btw`) run with `auto_approve: true`, bypassing both the approval modal and the persisted `/whitelist` — arbitrary `shell`/`powershell` execute with no prompt. `→ src/app/multichat.rs`
- `[BUG]` review-2026-08-26 #8: harness attributes a `ToolError` to `tools.last_mut()` instead of correlating by `(conversation, tool_id)` — wrong per-tool metrics with more than one tool per step or concurrent sub-agents sharing the event channel. `→ src/harness/driver.rs`
- `[BUG]` review-2026-08-26 #9: `--config` does not redirect `Config::data_dir()` — a harness run still writes to the real sessions dir, reads the real whitelist, and shares the semantic index with the live TUI, contrary to the function's own doc comment promising a throwaway data set. `→ src/config/mod.rs`

- `[BUG]` compaction-review-2026-08-26 #9: `strip_long_code_blocks` collapses assistant code fences ≥300 chars to `[...]` whenever history is replayed as `LOCAL MEMORY` — so rung 2's re-seed silently loses code the model itself wrote, at the one moment the whole local history goes on the wire. `→ src/provider/prompt.rs` (`format_history_message`)
- `[BUG]` compaction-review-2026-08-26 #10: sub-agents get rung 0 only. `run_sub_agent` takes `tool_output_limit` but no `ContextSpec`, so there is no measurement and no rungs 1-3 — a chain at the defaults (8 steps × 10 tools × 10 000 chars) reaches ~266k budget tokens with nothing watching. The §3 warning in `.docs/context-compaction.md` predicted exactly this and is still open. `→ src/agent/sub_agent.rs`, `src/agent/runner.rs`

- `[BUG]` list-refactor-2026-08-27 #1: `render_mcp_list` вне `auth_mode` не рисует ничего при пустом списке серверов — ни строки, ни сообщения. В `auth_mode` сообщение есть. Не следствие рефакторинга списков, отложено осознанно: это добавление UI. `→ src/tui/render/mcp.rs` (`render_mcp_list`)

- `[BUG]` phase2-2026-08-27 #1: припаркованный вопрос (`PendingInteraction::Question`) нельзя вычистить при отмене хода — `purge_interactions_for` фильтрует только подтверждения, потому что `QuestionRequest` не несёт `ConversationId` (в отличие от `ToolApprovalRequest`). Дефект существовал и до выделения `app/interaction.rs`. `→ src/app/interaction.rs` (`purge_interactions_for`), `src/app/events.rs` (`QuestionRequest`)
- `[BUG]` phase2-2026-08-27 #2: оборвавшийся ход параллельного чата не на экране пропадает молча — `turn_tail` возвращает `TurnTail::None`, и ошибка не попадает ни в строку состояния, ни в сам чат. Поведение унаследовано от удалённого `handle_background_event`, теперь его видно в одном месте. `→ src/app/reduce.rs` (`turn_tail`)

## MEDIUM

- `[BUG]` MCP tool arguments are passed to `tools/call` with **no schema validation**. `→ src/mcp/client.rs`
- `[BUG]` `stream_visible_text` truncates at the first bare `<` → legitimate text containing `<` (C++ templates, `a < b`, HTML) is hidden mid-stream. `→ src/agent/tool_parser.rs`
- `[BUG]` PoW challenge is solved once before the retry loop, not re-solved per attempt — deliberately left (changing it changes the request pattern); a stale challenge on a slow retry can be rejected. (~~solve ran on the async task~~ — fixed 2026-07-04, now `spawn_blocking`.) `→ src/provider/pow.rs` + `src/provider/deepseek/stream.rs`
- `[BUG]` Legacy `[TOOL:name] {json}` regex uses a non-nesting brace pattern and can't parse nested JSON objects. `→ src/agent/tool_parser.rs`
- `[BUG]` Fenced code-block examples containing tool-call syntax can be parsed and executed as real tool calls during auto-approve (background) turns. `→ src/agent/tool_parser.rs`
- `[BUG]` review-2026-08-26 #10: check-then-add on the shared shell output budget (load → compute → `fetch_add`, three unsynchronized steps across two readers) can overshoot the 1 MiB cap by up to one chunk. `→ src/tools/shell.rs`
- `[BUG]` review-2026-08-26 #13: tests write into the real `Config::sessions_dir()` (a non-injectable global) — parallel runs share state, a failing test leaves files behind. **Scope corrected on re-verification: 1 file, 4 tests** (`plant_session_file` and its callers), not the review's "~13 other files" — every other `data_dir()`/`sessions_dir()` call site is production code or an `#[ignore]`d model-cache test. Fixing it is still tied to #9 (no data-dir redirect). `→ src/session.rs`
- `[BUG]` compaction-review-2026-08-26 #12: background sub-agent results never enter the harness transcript. The TUI's `finish_background` flushes the answer into the parent chat's messages; `drive` only removes the id from `pending`, because every history-applying arm is behind `conversation == root`. Latent until a multi-turn scenario spawns a background sub-agent. `→ src/harness/driver.rs`
- `[BUG]` compaction-review-2026-08-26 #13: `multi-turn-history.toml` can pass with the history dropped — no `tools_forbidden`/`max_tool_calls`, so a turn-3 re-read is undetectable; `expect.max_steps = 24` equals the reachable ceiling (`max_steps = 8` × 3 turns) and can never trip; `min_pass_rate = 0.66` with `repeat = 2` is arithmetically 1.0. `→ sandbox/scenarios/live/multi-turn-history.toml`
- `[BUG]` compaction-review-2026-08-26 #14: multi-turn metrics mix two combining rules — `steps`/`tool_calls` sum across turns while `turn_end` and `semantic_hint` are plain assignments, so a 3-turn run whose turn 1 failed reports turn 3's outcome. `→ src/harness/metrics.rs` (`from_trace`)
- `[BUG]` compaction-review-2026-08-26 #15: the run deadline is armed inside `drive`, i.e. after `assemble`, so a cold semantic index eats wall clock the child's own `--timeout` never sees; past the parent's `timeout + 60` backstop the repeat is recorded as `setup_failed`, not `timed_out`. `→ src/harness/driver.rs`, `src/harness/scenario.rs` (`run_repeat`)
- `[BUG]` compaction-review-2026-08-26 #18: `auto_compact` is documented as the ladder's master switch, but rung 0 (`cap_tool_output`) is applied unconditionally in both `run_agent_loop` and `run_sub_agent` — only `tool_output_limit = 0` disables it, and that is documented nowhere. `→ src/agent/runner.rs`, `src/agent/sub_agent.rs`, `src/config/mod.rs`
- `[BUG]` compaction-review-2026-08-26 #19: mode 2's chunk loop `?`s on both the model call and `validate`, so a failure on chunk N discards the N-1 model calls already paid for and reports a single reason. `→ src/context/compact.rs` (`compact`)
- `[BUG]` compaction-review-2026-08-26 #20: `TOOL_RESULT_CHARS_FOR_SUMMARY` (2000, named "chars") is applied through `util::truncate_with_ellipsis`, whose parameter is `max_bytes` — Cyrillic tool output reaches the summariser at half the intended budget, and rung 0's `cap_tool_output` counts chars, so the ladder's two output limits are in different units. `→ src/context/summary.rs` (`render_transcript`), `src/util.rs`
- `[BUG]` compaction-review-2026-08-26 #21: `observed_facts` emits tool **names** only (`## Tools Used\n- shell, read_file`); §5.4 promises files, commands and error strings, and the measured 0.2% command survival is the whole reason that section exists. Compounded by `strip_tool_calls`, which removes the arguments from history before anything could extract them. `→ src/context/summary.rs` (`observed_facts`), `src/agent/tool_parser.rs`
- `[BUG]` compaction-review-2026-08-26 #22: `/default-compact` writes `[context] compact_mode` to disk through `save_config_then`, but nothing reloads `App.config` (only `CommandResult::ResetProvider` does), so the new default does nothing until restart. `→ src/commands/defs/default_compact.rs`, `src/commands/mod.rs` (`save_config_then`)
- `[BUG]` compaction-review-2026-08-26 #23: with `usable == 0` — the default on DeepSeek, which reports no window — `spawn_compaction` passes 0, `MODE1_HEAD_SHARE` yields a zero threshold, and mode 1 silently behaves as mode 3 while the status line says "mode 1". Noted in `.docs/context-compaction.md` §3.1, never surfaced to the user. `→ src/app/multichat.rs` (`spawn_compaction`), `src/context/modes.rs` (`plan`)
- `[BUG]` compaction-review-2026-08-26 #24: `Conversation.compact_mode` is not persisted and `handle_load_session` does not reset it, so a mode picked for one chat governs a different session loaded into the same slot. `→ src/app/conversation.rs`, `src/app/sessions.rs` (`handle_load_session`)
- `[BUG]` compaction-review-2026-08-26 #25 — **two thirds closed 2026-08-26, scope narrowed to the last third.** Order *is* now enforced (each heading is searched from where the previous one ended) and `## Work State`'s three subsections are part of `REQUIRED_SECTIONS`, so the flat and the shuffled form both fail — see RESOLVED. What remains: a reply echoing the `<template>` block verbatim still passes, because the placeholder text sits under headings in the right order and nothing distinguishes `- [one or two sentences: …]` from content. Cheap guard if it ever bites: reject a reply whose bullets are the template's own bracketed prompts. `→ src/context/summary.rs` (`validate`, `TEMPLATE`)
- `[BUG]` compaction-review-2026-08-26 #27: §3 promises "the tool call stays, only the body goes", but the runner stores `ChatMessage::assistant(&visible_text)` with `strip_tool_calls` already applied — only `ChatMessage.name` on the result survives, and both `compat_client` (`[tool result]\n…`) and `prompt.rs`'s history path (`[TOOL]\n…`) drop that too. `→ src/agent/runner.rs`, `src/provider/compat_client.rs`, `src/provider/prompt.rs`
- `[BUG]` compaction-review-2026-08-26 #28: `SessionReset` reports `before_tokens` from the provider's own session tally and `after_tokens` from `conversation_tokens` — two different measures presented to the user as one before/after comparison. `→ src/agent/runner.rs` (`reset_server_session`)
- `[BUG]` compaction-review-2026-08-26 #29: the GOAL evaluator calls `complete()` on the focused conversation's provider handle **unforked**; on DeepSeek its two-message request reads as a first conversational send, so it mints a fresh server session and zeroes `session_tokens` mid-conversation — the very number the ladder acts on. `→ src/app/goal.rs` (`spawn_goal_evaluation`)
- `[BUG]` review-2026-08-26 #14: `AppError::Custom(String)` has no `#[source]`, so the error chain is flattened into a string at all 39 `Custom(...)` sites. **Count corrected on re-verification:** only 4 sites are the bare `Custom(e.to_string())` the review describes (`debug_log.rs` ×2, `deepseek/endpoints.rs`, and `error.rs`'s own `From<Report>`), not 24 — most sites already add the path and the operation via `format!`. The lost `io::ErrorKind` is real; the "no file, no operation" framing is not. `→ src/error.rs`

## LOW

- `[BUG]` `/import` overwrites the session tag with `"Imported"`, losing any original tag/metadata. `→ src/commands/defs/import.rs`
- `[BUG]` Autocomplete file paths resolve against `current_dir()`, which can drift from `workspace_path` after a subprocess `cd`. `→ src/app/keys/autocomplete.rs`
- `[BUG]` `RagLimit` deserializer contract split: `visit_u64(0)` silently floors to `Fixed(1)` while `visit_i64(0)` hard-errors — behavior depends on which visitor the TOML backend invokes. Found 2026-07-15. `→ src/config/mod.rs`
- `[BUG]` `Conversations::remove` leaves `self.focused` dangling if the removed conversation is both focused and last → next `focused()` panics via `.expect`; guarded only by today's two call sites. Found 2026-07-15. `→ src/app/conversation.rs`
- `[BUG]` ACP mode: `PromptRequest.images` is parsed then silently dropped; stdout write failures are `.ok()`-swallowed so a half-closed pipe leaves the loop running completions and discarding responses forever. (ACP also has no tools and a hardcoded system prompt — owner decision pending, see AUDIT-2026-07-15.) Found 2026-07-15. `→ src/acp/server.rs`
- `[BUG]` MCP image content is dropped to `[Image: {mime}]` text; non-text/image/resource content types ignored. `→ src/mcp/client.rs`
- `[BUG]` `mcp__` uses `__` as separator → name collision possible if a server/tool name contains `__`. `→ src/mcp/manager.rs`
- `[BUG]` History deduplication only catches **consecutive** duplicates. `→ src/session.rs`
- `[BUG]` `"model"` field in the outgoing request body is a hardcoded literal `"deepseek-chat"`, ignoring the user's configured model. `→ src/provider/deepseek.rs`
- `[BUG]` Foreground child PID tracking is a single global slot (`AtomicU32`), not per-conversation — Esc/Ctrl+C in one conversation can kill a different conversation's foreground process; also an upward `tools`→`app` dependency. `→ src/app/mod.rs` (`FOREGROUND_CHILD_PID`) + `src/tools/shell.rs`
- `[BUG]` review-2026-08-26 #18: `copy_tree` (recursive `std::fs`, no `spawn_blocking`) runs inside the async `execute`, in the setup loop before each `tokio::spawn`. Blocks the worker polling `execute` and serialises the setup loop; on the multi-thread runtime already-spawned siblings keep running, so the review's "blocks the whole suite" overstates it. `→ src/harness/scenario.rs`
- `[BUG]` review-2026-08-26 #19 (informational): UTF-16 detection samples only the first 512 bytes and treats any byte < 0x20 as implausible in text, including ESC — dense-ANSI PTY output or a mid-stream encoding change can decode wrong. `→ src/util.rs`
- `[BUG]` review-2026-08-26 #20 (informational): `/serve` bearer-token comparison uses `==`, not constant-time — a timing channel on the key, exploitable only over a non-loopback exposure. `→ src/server/http.rs`
- `[BUG]` compaction-review-2026-08-26 #30: `turns` counts turns *started*, and `final_text` only updates on `AgentDone` for the root — a run that times out during turn 2 reports `turns = 2` with turn 1's answer as its final text. `→ src/harness/driver.rs` (`drive`)
- `[BUG]` compaction-review-2026-08-26 #31: `.memories/reference/CONFIG.md` and `README.md` still document the removed `[agent] max_context_messages` and the moved `auto_compact`, and neither mentions `[context]` at all; `sandbox/config.template.toml` has no `[context]` section either. `→ .memories/reference/CONFIG.md`, `README.md`, `sandbox/config.template.toml`
- `[BUG]` compaction-review-2026-08-26 #32: `cap_tool_output`'s result is always `limit + ~83` chars, so it *grows* its input whenever the overflow is smaller than its own marker (~83 chars) — the trade rung 1 now guards with `MIN_BODY_MARKER_RATIO`, missing at rung 0. `→ src/context/tool_output.rs`
- `[BUG]` compaction-review-2026-08-26 #33: the `ToolOutputCleared` arm writes `status_message` unconditionally, so a background turn's clearing overwrites the status of the chat the user is reading; the `SessionReset` arm directly below it guards on `focused_id()`. `→ src/app/mod.rs` (`handle_event`)
- `[BUG]` compaction-review-2026-08-26 #34: nothing ever deletes `data_dir/tool-output/**` — `ContextSpec::new` creates a per-session subdirectory and no cleanup path exists anywhere in the codebase. `→ src/context/spec.rs`
- `[BUG]` compaction-review-2026-08-26 #35 (informational): `read_file` reads any absolute path with no workspace restriction, and it is the tool the spill marker instructs the model to call. **Consistent with the project's existing trust model** — `bash`/`powershell` already run unrestricted by design (see WONTFIX below), so this is not a new capability; noted because the marker turns it into a routine, model-initiated read rather than one the user asked for. `→ src/tools/read_file.rs`
- `[BUG]` compaction-review-2026-08-26 #36: `context_used` is not cleared by `/clear` (`clear_chat_view` empties `messages` and leaves the counter), nor by `handle_load_session` — the `ctx:` indicator keeps reporting the old conversation's fullness until the next request re-measures. `→ src/app/mod.rs` (`clear_chat_view`), `src/app/sessions.rs`

## WONTFIX / ACCEPTED

- `[?]` compaction-review-2026-08-26 #37 — `budget_tokens` uses `chars / 3`, which still underestimates Cyrillic (~2 chars/token) and CJK (3-4× off), so the thresholds fire late on exactly the text the constant was picked to protect. **Owner decision 2026-08-26: keep 3 and wait for real `prompt_tokens`** rather than adding a tokenizer or a per-script factor. Recorded as a known limitation, not a defect to fix. `→ src/context/budget.rs` (`CHARS_PER_TOKEN`)

- ❌ **review-2026-08-26 #17 — REFUTED on re-verification.** The review claims `set_current_dir` (`driver.rs:236`) races live background tasks. It does not: nothing is spawned before that line — `MCPManager::new()` is at `:326`, `SemanticService::start` at `:341`, the first `spawn(TurnSpec)` at `:432`, and `exec` is a fresh process per run. The chdir stays process-global, which would matter only if `exec` ever ran in-process; not a defect today. `→ src/harness/driver.rs`

- `[?]` PoW runs DeepSeek's own wasm blob via `wasmtime` (heavy dep, repo blob). Native SHA-3 reimpl REJECTED by owner (2026-07): the workaround must execute upstream's solver as-is. The wasm is now embedded via `include_bytes!` (2026-07-03; a file in `assets/` still overrides for dev drop-ins). Remaining stretch: fetch the server-referenced wasm at runtime.
- `[?]` DeepSeek web API is reverse-engineered; may break on any server update. No SLA.
- `[?]` `bash`/`powershell` run arbitrary commands with no sandbox — by design; trust = tool-approval + `/whitelist`.

## RESOLVED / MOOT

- ✅ **compaction-review-2026-08-26 #4 — the context window was polled once at startup and never again** (found and fixed 2026-08-26). `App::new` asked `context_window()` on the provider it built and nothing ever asked again, so after `/providers` or `/models <id>` the ladder went on acting on the previous provider's number — measurement is the one layer that must not confidently report a wrong value. Every provider/model switch now goes through one function, `switch_provider`: it repoints the focused conversation, mints a fresh session id, and calls `invalidate_context_window` **synchronously** — the stored window drops to 0, which reads as "unknown" and switches the ladder off, the safe direction — before spawning the re-poll off the loop (invariant 1). A slow answer from the provider that was switched away from cannot land: `provider_window_epoch` is bumped by the invalidation and the spawned poll only sends `ContextWindowLearned` while its epoch still matches. Coverage is by construction, since all four paths already funnelled into `rebuild_provider`: `/providers` activate and remove (`app/keys/providers.rs`), the model picker (`app/keys/modal.rs`) and `/models <id>` (`app/providers.rs`) via `apply_model_switch`, and `CommandResult::ResetProvider` (`app/keys/dispatch.rs`). The one path that cannot re-poll — a model switch whose config save failed, so the handle still holds the old model — calls `invalidate_provider_context_window` and stops at unknown. Regressions: `switching_provider_forgets_the_old_window_and_re_polls`, `a_provider_that_cannot_say_leaves_the_window_unknown`, `a_slow_answer_from_the_previous_provider_is_dropped`. `→ src/app/mod.rs` (`switch_provider`, `invalidate_context_window`, `spawn_window_poll`, `provider_window_epoch`), `src/app/providers.rs` (`apply_model_switch`)
- ✅ **compaction-review-2026-08-26 #5 — moving `auto_compact` from `[agent]` to `[context]` switched the ladder back on for everyone who had turned it off** (found and fixed 2026-08-26). There is no `deny_unknown_fields`, so the stale key parsed fine and was dropped on the floor, `[context]` was absent, and the default is `true` — an upgrade silently reversed a deliberate answer, and the project's own test asserted the reversal. `config::parse` is now the single entry point for reading config text (`load`, `load_from`, and therefore `--config`, all go through it) and runs `apply_migrations` after the normal deserialize. The migration re-reads the raw text through `LegacyKeys`/`LegacyAutoCompact` probe structs whose field is `Option<bool>`, not `bool` — that is the whole trick, because only an `Option` tells an absent `[context] auto_compact` from an explicit `auto_compact = false`. An explicit `[context]` value always wins and nothing is announced; a legacy `[agent]` value is carried over only when `[context]` sets none, and then `Config.migration_notices` (a `#[serde(skip)]` field, never a config key) carries a line that `App::new` pushes as a `ChatMessage::ui_system` — the setting moved, your value was kept, the next save rewrites it under `[context]`. No `deny_unknown_fields` was added: an old config must keep loading. Regressions: `legacy_auto_compact_under_agent_section_is_migrated_and_announced`, `legacy_auto_compact_true_is_migrated_too`, `an_explicit_context_auto_compact_outranks_the_legacy_key`, `a_config_with_neither_key_is_untouched`, `loading_a_real_file_applies_the_migration` (the wiring, not just the parser). The old test that encoded the flip is gone. `→ src/config/mod.rs` (`parse`, `apply_migrations`, `LegacyKeys`, `migration_notices`), `src/app/mod.rs` (`App::new`)
- ✅ **compaction-review-2026-08-26 #6 — the prior summary reached the summariser twice** (found and fixed 2026-08-26). It went in as `<prior-summary>` *and* stayed in the working history, so it was inside `<conversation>` too: a repeated `/compact` paid a model call to summarise a summary against itself. `compact()` now takes the summary message **out of the working copy** before planning (`Cow`, so the no-summary case still borrows), which means it reaches the model exactly once, in the slot meant for it. Detection was tightened at the same time, because "starts with `[Context summary]`" is a string any message can carry: `prior_summary` requires role `User`, **not** `ui_only`, the `SUMMARY_PREFIX`, **and** a body that passes `summary::validate` — an assistant quoting the marker back, or a user pasting it, is not a summary of ours. The visible consequence: `/compact` twice with no turn in between ends with "Nothing to compact yet — there is only one turn." **before any model call**, where before it bought a second summary of the first. Regressions: `the_previous_summary_is_not_part_of_the_material_it_summarises` (asserts the prefix is absent from the `<conversation>` block, present as `<prior-summary>`, and that the rewritten history carries exactly one summary), `compacting_again_with_no_turn_between_costs_nothing` (asserts `fake.request(0).is_none()` — the summariser was never called), `only_a_summary_we_wrote_counts_as_the_prior_one`. `→ src/context/compact.rs` (`compact`, `prior_summary`, `SUMMARY_PREFIX`)
- ✅ **compaction-review-2026-08-26 #7 — mode 2 produced N stacked copies of the form instead of one summary** (found and fixed 2026-08-26). `summaries.join("\n\n")` handed the next agent eight `## Objective` headings and eight contradictory `## Next Move` lists. New `summary::merge` folds the chunk summaries section-wise into a single instance of the form: `section_lines` splits each chunk under the eight `REQUIRED_SECTIONS` headings (lines under any other heading belong to no section and are dropped), duplicate lines are dropped, `(none)`/empty placeholders never accumulate (`is_placeholder` strips list markers first, so `- (none)` and `1. (none)` count), an empty section gets exactly one `(none)` back — except `## Work State`, which only holds subsections — and `## Next Move` is taken from the **newest chunk that has a real one**, because several "do this next" lists in a row are contradictory orders, not a summary. Zero extra model calls. **A reduce pass was considered and rejected**, and the reason is recorded in the function's own doc comment: it would add a second all-or-nothing rejection point on top of the existing per-chunk `validate`, so a rejected reduce would discard every chunk call already paid for — which is the failure #7 describes, one level up. Regressions: `merged_chunks_are_one_form_that_keeps_what_each_chunk_reported` (every heading exactly once, every chunk's file present, the newest real next move wins and the older one is gone, three placeholders and not nine), `a_single_chunk_is_passed_through_untouched`, and end to end `many_chunks_still_produce_a_single_summary` in `compact.rs`, which asserts each heading appears once in the summary the history actually receives and that no chunk was dropped by the merge. `→ src/context/summary.rs` (`merge`, `section_lines`, `is_placeholder`, `NEXT_MOVE`, `WORK_STATE`), `src/context/compact.rs`
- ✅ **compaction-review-2026-08-26 #8 — every mode-2 chunk shared one provider session, so on DeepSeek chunk N was answered in the context of chunks 1..N-1** (found and fixed 2026-08-26). Same class as the `/compact` branch-pollution defect fixed hours earlier, one level down: the throwaway fork kept its server-side branch across the whole loop, so later chunks saw earlier chunks' transcripts upstream and summarised material that was not theirs. Now each chunk after the first calls `discard_remote_session()` then `reset()` before its request, and only when `provider.keeps_server_side_history()` — a local-history provider pays nothing. `compact`'s doc comment states the precondition the fix depends on: the handle **must** be a throwaway fork, because this drops sessions on it. Regression: `every_chunk_is_summarised_in_its_own_session` asserts `fake.resets() == calls - 1` against a `FakeProvider::server_side_history()`. `→ src/context/compact.rs` (`compact`), `src/provider/fake.rs` (`server_side_history`, `resets`)
- ✅ **compaction-review-2026-08-26 #11 — the harness driver ignored `ToolOutputCleared`** (fixed 2026-08-26 alongside the `[context]` scenario table; the documenting pass that closed #1/#2/#3/#16/#17 left this entry open because it was not on its list, and it is closed here on re-verification). A multi-turn harness history kept the bodies the TUI would have replaced with markers — the driver diverged from the TUI in exactly the dimension compaction is about, so turn 2 measured a history no user would ever have. `drive`'s event loop now has a `ToolOutputCleared` arm that calls `apply_cleared_tool_output` when the event belongs to the root conversation, and traces `cleared`/`freed_tokens` either way. The matching is **by tool-call id, never by index**, and the doc comment says why: the agent loop works on a copy with the `ui_only` messages dropped, so the two lists never line up. Regressions: `cleared_tool_output_is_applied_the_way_the_tui_applies_it`, `an_unknown_tool_call_id_clears_nothing`. Asserted end to end too — `rung-one-clears-tool-output.toml` requires at least one `harness.context.tool_output_cleared` record with `cleared ≥ 1`. `→ src/harness/driver.rs` (`drive`) — с 2026-08-27 логика живёт в `app::reduce::apply` / `Conversation::clear_tool_output`, своей копии в харнессе больше нет
- ✅ **compaction-review-2026-08-26 #26 — mode 1 dropped everything ahead of the first user message, and #25's order/subsection halves** (found and fixed 2026-08-26; two small ones from the same round). Mode 1 planned `head = starts[0]..starts[1]`, so `messages[0..first]` — anything before the first non-`ui_only` user message — was neither kept nor summarised, while modes 2 and 3 started at 0 and were fine. `plan` now takes `0..starts[1]`, with the reason in the code: whatever precedes the first user message is history too. Regression: `no_mode_silently_drops_a_message` builds a history led by a system message and asserts, for all three modes, that `head ∪ summarize ∪ tail` covers every index — a coverage check rather than a per-mode assertion, so it catches the next mode as well. Separately, `summary::validate` stopped using `contains`: `REQUIRED_SECTIONS` grew from five to **eight** (the three `## Work State` subsections are part of the form — a Work State without them is not one) and each heading is now searched from where the previous one ended, so order is enforced as the doc comment always claimed. Regressions: `a_work_state_without_its_subsections_is_not_a_work_state`, `the_form_is_refused_when_its_sections_arrive_out_of_order`, `a_truncated_reply_is_refused_rather_than_half_used`. The third part of #25 — a verbatim `<template>` echo passes — is **still open** and #25 stays in MEDIUM with its scope narrowed to that. `→ src/context/modes.rs` (`plan`), `src/context/summary.rs` (`validate`, `REQUIRED_SECTIONS`)

- ✅ **compaction-review-2026-08-26 #1 — no scenario could reach rung 1, 2 or 3; the ladder shipped with no automated test at all** (found and fixed 2026-08-26). Four things had to change before a scenario could even measure: `Scenario` gained a `[context]` table (`window`, `reserved_tokens`, `preserve_recent_tokens`, `tool_output_limit`, `auto_compact`), deserialised straight into `driver::ContextOverrides` so TOML keys and flags cannot drift; `exec` gained the matching flags (`--context-window`, `--context-reserved-tokens`, `--context-preserve-recent-tokens`, `--tool-output-limit`, `--auto-compact`), since every repeat is a separate process; `ContextOverrides::apply` layers them over the loaded config before anything reads it, and every field is optional, so a scenario without the table passes no flag and the child's own config keeps deciding; and the driver stopped hardcoding `provider_window: 0` — it now asks the provider in the background exactly as `App::new` does, records the answer as `harness.context.window`, and feeds it to both `ContextSpec::new` sites, with config still outranking it. `sandbox/config.template.toml` gained a `[context]` section (which also closes the `config.template.toml` half of #31). **Evidence, and the part that matters:** the new `sandbox/scenarios/mock/rung-one-clears-tool-output.toml` passes 2/2 with its `[context]` table, and with the window removed from **both** the scenario and the config it fails — `trace has 0 'context.prune' record(s)`, exit code 4. The test can fail; that is what makes it worth having. Mock and not live on purpose: `mock-provider` goes through `CompatClient`, so `keeps_server_side_history()` is false and rung 1 applies, where every DeepSeek scenario only ever produces `context.prune.skipped`. `→ src/harness/driver.rs` (`ContextOverrides`, `drive`), `src/harness/scenario.rs`, `src/harness/mod.rs`, `src/harness/mock.rs` (`context_length` in the model listing), `sandbox/config.template.toml`, `sandbox/scenarios/mock/rung-one-clears-tool-output.toml`
- ✅ **compaction-review-2026-08-26 #2 — a scenario reported PASS when the run never started** (found and fixed 2026-08-26). Every `Expect` field is optional and `Expect` is `#[serde(default)]`, so a `setup_failed` child cleared every check and `run_one` exited 0: `1/1 passed (100%)` over `status: setup_failed, turns: 0`. `Expect::check_ran_at_all` now fails a run that produced nothing to judge — `SetupFailed`, zero turns, or turns that started but ran no step — and the message carries the child's error and names the status to declare. The waiver is explicit and per status: only `status = "failed" | "timed_out" | "setup_failed"` excuses it, so `fails-cleanly-on-429` keeps working while a scenario expecting `completed` cannot inherit the exemption. Regressions: `a_run_that_never_started_fails_even_with_no_expectations`, `a_completed_run_with_no_steps_is_still_nothing_to_judge`, `expecting_completion_does_not_excuse_a_run_that_never_started`, `a_scenario_can_opt_in_to_expecting_a_failed_run`. The one committed scenario that had been passing vacuously — `sandbox/scenarios/live/reports-denied-tools.toml`, no `status`, `max_steps` and a `final_not_matches` regex both satisfied by an empty run — needs no edit: it wanted a real run all along and now has to get one. Same pass also added `[[expect.trace]]` (`action`, `min_count` defaulting to **1**, `max_count`, `field`, `min_total`, `max_total`), which is what lets a scenario assert a rung *fired* rather than that the run finished; an assertion that asserts nothing (a `field` with no bound, a bound with no `field`) is rejected at load time. `→ src/harness/scenario.rs` (`Expect::check`, `check_ran_at_all`, `check_trace`, `TraceExpect`)
- ✅ **compaction-review-2026-08-26 #3 and #16 — the 20000-token reserve switched the whole ladder off on small windows, and `max_tokens` was never consulted** (found and fixed 2026-08-26; one fix closes both). `usable()` returned `None` whenever the flat `reserved_tokens` swallowed the window, so on any window ≤ 20k — exactly the models that need compaction — the ladder and the `ctx:` indicator went silently dark. The reserve is now what `.docs/context-compaction.md` §4 always promised: `min(reserved_tokens, max_tokens)` (`ContextSpec::with_output_cap` → `bound_reserve`; `max_tokens = 0` means "unknown cap", leaving the configured value), and `ContextBudget::reserve()` caps it at half the window regardless — a reserve must never leave the conversation less room than the answer it holds back. `usable()` therefore returns `None` only for an unknown window: an 8k window with the default 20k reserve and `max_tokens = 4096` yields **4000** usable instead of nothing. Applied at all four `ContextSpec::new` sites (`app/mod.rs`, `app/multichat.rs`, both in `harness/driver.rs`), and `spec::budget_from_config` gives the two callers outside a turn — the status bar and `/compact` — the same rule from one place instead of a copy each. Regressions: `a_small_window_keeps_the_ladder_on`, `the_reserve_never_takes_more_than_half_the_window`, `max_tokens_bounds_the_reserve_of_a_turn`, `an_unknown_output_cap_leaves_the_configured_reserve_alone`, `a_turn_on_a_small_window_still_gets_a_budget`, `the_status_bar_path_applies_the_same_reserve_rule`. `→ src/context/budget.rs` (`reserve`, `usable`), `src/context/spec.rs` (`with_output_cap`, `bound_reserve`, `budget_from_config`), `src/tui/render/status.rs`, `src/config/mod.rs` (doc comment)
- ✅ **compaction-review-2026-08-26 #17 — the `ctx:` indicator printed a denominator it did not divide by** (found and fixed 2026-08-26). `percent_used()` divided by `usable` while `label()` printed the raw `window`, so 54000 used of a 128k window with a 20k reserve rendered as "50% of 128k" — two different windows in one string. `label()` now prints `usable`, the scale every ladder threshold is written against ("50% of 108k*"), and `BudgetSnapshot.window` is gone rather than left as a second number to pick the wrong one of. The `min(100)` clamp went with it: over budget the reading now runs past 100 ("150% of 8k*", capped only at `u8::MAX`), because a user whose conversation is 50% over its budget needs to see that, not a pinned 100%. The test that encoded the old behaviour (`label_marks_a_hand_set_window`) is rewritten as `label_prints_the_denominator_percent_used_divides_by`; `percent_used_reports_how_far_past_the_budget_it_is` covers the clamp. `→ src/context/budget.rs` (`BudgetSnapshot::label`, `percent_used`)

- ✅ **`/compact` had no guard against a running turn or a second `/compact`, and `CompactFinished` overwrote the history wholesale** (found 2026-08-26 by the five-agent compaction review, fixed the same day). The task rewrote a *snapshot* of the history taken at spawn time, so anything the user or a live turn added while the summariser ran was destroyed on arrival. Now `Conversation.compacting` holds the snapshot length: `spawn_compaction` refuses while the chat is streaming or already compacting, `end_compaction`/`swap_compacted` splice the newcomers onto the rebuilt history and refuse the swap outright if the history shrank underneath, the `JoinHandle` is stored so Ctrl+C/Esc can abort the run (which then reports "cancelled" rather than applying stale messages), and `swap_compacted` refreshes `context_used` so the indicator matches the new history. `→ src/app/conversation.rs` (`begin_compaction`/`end_compaction`/`swap_compacted`), `src/app/multichat.rs` (`spawn_compaction`), `src/app/mod.rs` (`CompactFinished`)
- ✅ **A failed rung-1 spill was silent, and the loss reached the saved session** (same review, same day). `clear_tool_bodies` replaced the body with a marker naming a file that had never been written — the model was told to `read_file` a path that did not exist, and the original output was gone from history for good. Now the spill task returns the indices it actually wrote, only those bodies are replaced, and `context.prune` carries `spill_failed=N`. Regression: `a_body_whose_spill_fails_is_never_replaced_by_a_marker`. `→ src/agent/runner.rs` (`clear_tool_bodies`)
- ✅ **Rung 2 cleared tool bodies *before* deciding whether the re-seed was worth it** (same review, same day), so both refusal paths — "still over usable" and a failed `reset()` — destroyed history for nothing, on a provider where a cleared body is never re-sent. The project's own test asserted that behaviour. Now the decision runs on a prospective estimate built from the pure `plan_prune`/`freed_tokens`/`marker_tokens` (nothing touched), and `clear_tool_bodies` runs only after `reset()` succeeds; the test is inverted to `assert!(!dir.exists(), "nothing may be spilled on the refusal path")`. `→ src/agent/runner.rs` (`reset_server_session`)
- ✅ **The rung-2 `still_over_usable` refusal repeated on every step of a turn** (same review, same day), re-running the whole estimate and — before the fix above — re-spilling each time. Now `CompactionState.reset_refused` makes a refusal stand for the turn, logged once. `→ src/agent/runner.rs` (`CompactionState`, `reset_server_session`)
- ✅ **Rung 1 had no minimum-body filter, so clearing a short result made the history longer** (same review, same day) — a body smaller than the marker that replaced it grew on "clearing", while `freed_tokens` cheerfully reported the old body as saved. Now `plan_prune` skips any victim whose body does not beat `marker_tokens` × `MIN_BODY_MARKER_RATIO` (2), and the floor is *derived* from the marker, path included, so it moves with the spill directory. Regression: `a_body_no_bigger_than_its_own_marker_is_left_alone`. `→ src/context/prune.rs` (`plan_prune`, `marker_tokens`)

- ✅ **`/compact` was a no-op on DeepSeek and polluted its live branch** (found 2026-08-26 by an agent reading the diff, hours after the code was written, not by a test). Two faults in one: `spawn_compaction` never checked `keeps_server_side_history()`, so on the project's main provider it rewrote a local history that is never resent — the same blind spot rung 1 had; and it summarised through the conversation's own provider handle, so the summariser prompt and its reply were written into the live server branch the user can open in the web UI. Fixed the same day: the summary now runs on a throwaway `fork()` that is discarded afterwards (`discard_remote_session`), and a successful compaction on a server-side-history provider calls `reset()` so the re-seeded session carries the summary instead of the branch it replaced. `→ src/app/multichat.rs` (`spawn_compaction`)
- ✅ **`SummaryError`'s doc comment described a fallback that does not exist** (same reading). It claimed the caller drops back to the mechanical rungs; no caller does — the history is left untouched and the reason is reported. Comment corrected to match. `→ src/context/summary.rs`

- ✅ **review-2026-08-26 #3 (Critical) — `max_context_messages`/`auto_compact` declared, defaulted, serialized, never read; no context trimming despite being configurable.** Closed 2026-08-26 by the whole compaction ladder (`.docs/context-compaction.md`, steps 1-6, all shipped the same day). `max_context_messages` is gone outright (step 1); `auto_compact` is rung 1's and rung 2's master switch (`ContextSpec.auto_compact`, `prune_tool_output`, `reset_server_session`). DeepSeek no longer "skips the whole ladder": rung 0 caps tool output at capture for every provider, and rung 2 re-seeds its server session above 90%, running rung 1's clearing first. **One qualification for the record:** rung 3 (the LLM summary) is manual by design — `/compact` — so "automatic trimming" means rungs 0-2 only; nothing summarises on its own, and that is the decision, not an omission. `→ src/config/mod.rs`, `src/context/`, `src/agent/runner.rs`
- ✅ **review-2026-08-26 #4 (High) — `/compact` was a stub presented as a feature** ("summary" = every user message's `content` joined with `"; "`, assistant/tool messages dropped, `.docs/TODO.md` listing it complete). Closed 2026-08-26: the stub is deleted and `/compact [1|2|3]` now runs rung 3 — three turn-aligned modes (`src/context/modes.rs`), a form-based prompt that is refused whole when a section is missing (`summary.rs`), one model call per chunk off the event loop (`compact.rs` + `App::spawn_compaction` → `AppEvent::CompactFinished`), per-chat mode plus `/default-compact` for the default. `.docs/TODO.md` corrected in the same pass. Caveat carried forward: never run live — unit tests over `FakeProvider` only — and see the two open HIGH entries above (DeepSeek no-op / branch pollution, stale doc comment). `→ src/commands/defs/compact.rs`, `src/commands/defs/default_compact.rs`
- ✅ **`pooprusteek exec --save-session` saved sessions with no assistant messages at all** (found 2026-08-26 while adding multi-turn scenarios). `harness/driver.rs`'s `drive` rebuilt its transcript from `AppEvent::AddMessage` only — but `run_agent_loop` never sends that event for assistant text, which streams as `AgentChunk` and is assembled into a `ChatMessage` by the TUI's own `Conversation::begin_assistant_message`/`append_chunk`/`discard_empty_assistant`. The driver listened for none of the three, so every session `exec --save-session` ever wrote was missing every answer the model gave — silently, since `RunOutcome` still reported the right `final_text` from `AgentDone`. Those files feed `harness mine --sessions` and the semantic history index, both of which had been training on a corpus with the assistant side removed since the harness shipped (2026-08-25). Fixed by having `drive` apply the same three events, to the same `Conversation` type the TUI uses, so a saved session (and turn two of a multi-turn run) sees exactly what the TUI would have recorded. Regression: `a_second_turn_is_sent_the_first_turns_history` (`src/harness/driver.rs`) asserts the second request literally contains the first turn's assistant answer. `→ src/harness/driver.rs`

- ✅ **2026-08-26 clippy pass (#15)** — `[lints.clippy]` added with `redundant_clone = "deny"` (`deny`, not `warn`: a specific level outranks `-D warnings`, as the `[lints.rust]` comment already documents; verified with a probe — reintroducing one clone fails the build with exit 101). Nine redundant clones removed (`app/keys/mcp.rs` ×2, `app/keys/onboarding.rs`, `app/providers.rs`, `commands/defs/cwd.rs`, `commands/defs/import.rs`, `server/openai.rs`, `tools/registry.rs`, `tui/render/mcp.rs`). Two corrections to the review: the `perf` group (and `large_enum_variant` inside it) is **already on by clippy's default** and clean, so there was nothing to add; `needless_collect` was **rejected** — all three hits are false, `collect()` there deliberately ends a borrow before a mutation or an `.await` (`mcp/manager.rs` documents exactly that in six lines). `#[allow]` → `#[expect]`: 10 attributes plus one module-level `#![allow]` the review's grep missed; one survives on purpose (`app/events.rs`, where `expect` would be unfulfilled under `cargo test` — stated in the code), and one dead attribute was deleted rather than converted (`app/system_prompt.rs::build` has exactly 7 args, below the lint's threshold). `→ Cargo.toml`, 10 source files

- ✅ **2026-08-26 review fix batch** — full-codebase review `.docs/review-2026-08-26-rust.md` (20 numbered findings), 6 closed same day:
  - **#1 (Critical) — `atomic_write` could lose data entirely on Windows, not just truncate it.** The `#[cfg(windows)]` remove-before-rename block claimed `std::fs::rename` fails when the target exists on Windows — false, `MoveFileExW`/`MOVEFILE_REPLACE_EXISTING` already replaces; the remove opened a window where a crash left no file at all. Also folds in **#12 (Medium)**: `File::sync_all()` before the rename, and a unique temp name (pid + `AtomicU64`) so concurrent writers to one path can't share a temp file. `→ src/util.rs` (`atomic_write`); consequence in `src/app/persist.rs`'s module doc, which no longer cites the fixed `.tmp` path.
  - **#2 (Critical) — a config.toml missing one section/key failed to parse and the TUI silently fell back to full defaults for the whole run.** `provider`/`ui`/`agent`/`mcp` got `#[serde(default)]` on `Config`; every scalar field of `ProviderConfig`/`UiConfig`/`AgentConfig` got `#[serde(default = "named_fn")]` (named functions, not bare `#[serde(default)]` — that would zero/false the real defaults). `Config` now derives `Default`. No `deny_unknown_fields`. `→ src/config/mod.rs`
  - **#5 (High) — CORS headers went out on every `/serve` response regardless of whether auth was configured**, while the bearer check only engages when `api_key` is set — any open web page could cross-origin read a loopback reply with no key configured. Now gated on `context.api_key.is_some()`. `→ src/server/http.rs`
  - **#11 (Medium) — bare `String::from_utf8_lossy` on child output inside the harness itself**, violating invariant 4 in code written after the invariant. Replaced with `util::decode_process_output`. `→ src/harness/scenario.rs`
  - **#16 (Low) — `sandbox/config.template.toml` advertised three `[ui]` keys removed from `UiConfig` in the 2026-07-15 sweepables pass.** Dropped. `→ sandbox/config.template.toml`
  - Plus, outside the review, **Stage 0 of `.docs/vision-local-image-understanding.md`**: `/attach`'s `submit_input` used `read_to_string(...).ok()?` inside a `filter_map`, so a non-UTF-8 file (e.g. a PNG) vanished entirely — status bar said "1 file attached", model got nothing. Moved to a pure `build_attachment_section` helper that always names the file and always emits a block (placeholder text for unreadable content, the OS error for an open failure). `→ src/app/keys/chat.rs` (`build_attachment_section`)
  - Tests 493→504 passing (508 total, 4 `#[ignore]`d), clippy 0, fmt clean. The other 14 findings are open — see CRITICAL/HIGH/MEDIUM/LOW above (prefixed `review-2026-08-26 #N`) and `.docs/review-2026-08-26-rust.md`. Full write-up: `JOURNAL/2026-08-26.md`.

- ✅ **Tool output in UTF-16 was decoded as UTF-8, handing the model mojibake instead of the error** (found 2026-08-25 by `harness mine` on its first trace corpus; fixed same day). On Windows `bash` commonly resolves to WSL, and `wsl.exe` writes *its own* diagnostics in UTF-16LE — a failed ext4.vhdx mount arrived as `1d 04 35 04 20 00 43 04 34 04`, unreadable to model and human alike. New shared `util::decode_process_output`: BOM first, then endianness inferred from which side of each byte pair carries a byte that cannot occur in text (< 0x20 excluding tab/CR/LF, or 0x7F). Counting NULs is not enough — `Н` (U+041D) is `1D 04`, so a Russian message has none; the first attempt at this heuristic failed its own test for exactly that reason. Both streaming readers now keep **raw bytes** (as their doc comments always claimed) instead of decoding per line and re-encoding — a UTF-16 newline is `0A 00`, so `read_until(b'
')` yields half-aligned chunks that cannot be decoded in isolation; one decode of the finished buffer replaces four scattered `from_utf8_lossy` calls. `→ src/util.rs` (`decode_process_output`), `src/tools/shell.rs` (`capped_pipe_reader` + collection site), `src/tools/background/spawn.rs`, `src/tools/background/types.rs` (`sanitize_terminal_output`)

- ✅ **Harness contaminated the workspace it was measuring** (found 2026-08-25 on the first tool-using run). `driver::exec` changed the process working directory into the scenario's workspace *before* resolving a relative `--trace` path, so the trace and its parent directories were created **inside the directory under test** — the agent then listed the harness's own scratch folder as part of the workspace. Trace paths are now absolutised before the `chdir`. `→ src/harness/driver.rs` (`absolutize`)

- ✅ **Quitting during the background MCP/semantic load left the process alive, holding the shell** (user-reported 2026-07-15, evening). Two compounding waits on exit: (1) `run()`'s `self.mcp.lock().await.shutdown_all().await` was the only unbounded cleanup step — now under the same 3s timeout as its neighbors (on timeout, children die with the process via the Windows kill-on-close Job Object / `kill_on_drop`); (2) the real killer — `#[tokio::main]`'s implicit runtime drop **waits for all `spawn_blocking` tasks**, and the semantic layer legitimately runs ONNX embedding there for seconds-to-minutes (model init/download, MCP-corpus re-embed, history backfill) — blocking work can't be aborted. `main` now builds the runtime manually and calls `shutdown_timeout(2s)` after `run_async` returns: abandoning the blocking threads is safe (persister already flushed bounded, every durable write is `atomic_write`, the semantic index is a rebuildable cache). Side effect worth knowing: a hung old instance used to keep its half-started `npx` MCP children alive, which could then collide with a relaunched instance's copies of the same servers (npm cache/port contention) — the "MCP acting up after a bad exit" symptom. `→ src/main.rs`, `src/app/mod.rs` (`run`)

- ✅ **2026-07-15 sweepables pass** — `write_history` now logs serialize/write failures; the skills toggle no longer reloads config from disk (a fresh-copy save used to silently clobber unsaved in-memory config changes); `/import`'s id-truncate is char-boundary-defensive; dead `cursor_pos` and the three never-read UiConfig fields deleted. `→ src/session.rs`, `src/app/pickers.rs`, `src/app/keys/modal.rs`, `src/commands/defs/import.rs`, `src/tui/widgets/input.rs`, `src/config/mod.rs`

- ✅ **2026-07-15 defect batch** — same-day fixes for the quality audit's defect findings (tests 434→436, clippy 0, fmt clean):
  - **MCP stdio children now bound to the Windows kill-on-close Job Object** (`win_job` re-exported from `tools/background`, assigned in `StdioTransport::new`) — a force-close can no longer orphan `npx …server-*` children. `→ src/mcp/transport.rs`, `src/tools/background/mod.rs`
  - **MCP view `'d'` (remove server) spawned off the event loop** and reports via `McpOperationDone`, matching toggle/reconnect — the manager lock is no longer held across process-close + config-write on the loop. `→ src/app/keys/mcp.rs`
  - **`tick_is_visual` unified**: checks the *focused* conversation only, and the `Tick` arm reuses the same predicate — a background sidechat no longer forces a full redraw every ~120 ms. `→ src/app/mod.rs`
  - **Sub-agents got the malformed-tool-call recovery**: `parse_tool_calls_with_errors` + the main loop's bounded retry (shared `MAX_MALFORMED_TOOL_RETRIES`/`malformed_tool_feedback`, now `pub(crate)` in runner.rs); exhausted retries return an explicit `Err` instead of a silent empty answer; `parse_tool_calls` demoted to a `#[cfg(test)]` helper. Regression test: `malformed_tool_call_is_retried_not_swallowed`. `→ src/agent/sub_agent.rs`, `src/agent/runner.rs`, `src/agent/tool_parser.rs`
  - **Semantic layer poison-tolerant**: new `lock_inner()` (`PoisonError::into_inner`) replaces every bare `.lock().unwrap()` on blocking paths; bounded event-loop paths keep treating poison as "busy, degrade". (Embedding still runs under the lock — hold-time only, degrade paths cover it.) Test: `poisoned_lock_recovers_instead_of_panicking`. `→ src/semantic/mod.rs`
  - **Server stats**: requests counted before the auth gate, so 401s can't push `errors > requests`. `→ src/server/http.rs`
  - **`/export` writes through `util::atomic_write`**. `→ src/commands/defs/export.rs`

- ✅ `~~Theme is hardcoded Catppuccin Mocha; `ui.theme` config is ignored~~` — fixed 2026-07-05 with the multi-theme system (`Theme::resolve(&config.ui)`, 10 presets, `/themes`, custom themes). Entry was stale in LOW until 2026-07-15.

- ✅ **Semantic hint silently displaced the user's message on the DeepSeek wire** (2026-07-13). `run_agent_loop` pushed the RAG hint as a `Role::System` message **after** the newest user message; `split_system_prompt` only extracts the *first* system message, and `build_prompt` on a continuing session sent **only `messages.last()`** — so whenever the hint fired, the request became `### USER INPUT\n[Hint…]` and the user's actual text never reached the server (on the first send it was mislabeled into `### LOCAL MEMORY` while the hint posed as USER INPUT). Symptom: model "hallucinating"/answering off-target exactly when semantic matching worked. **Fix:** (1) hint now inserted *before* the user message (`runner.rs` `rposition`+`insert`); (2) `build_prompt` rewritten around a **tail batch** — everything after the last assistant message goes out, each piece as its own section (`### NOTE` for system notes, `### USER INPUT`, `### TOOL RESULT: …`), so a user message can never be displaced regardless of ordering; (3) `should_reset` in `stream.rs` now uses role-aware `is_first_conversational_send` (a hint no longer masks a fresh conversation); (4) every non-empty send gets a trailing one-line `FORMAT_REMINDER` (recency anchor for the tool-call format). Regression tests: `hint_before_user_keeps_user_input_last`, `trailing_system_note_never_displaces_user_input`, `first_send_renders_hint_as_note_not_memory`. `→ src/provider/prompt.rs`, `src/provider/deepseek/stream.rs`, `src/agent/runner.rs`. Write-up: `JOURNAL/2026-07-13.md`.

- ✅ **Frozen agent on malformed `<tool_use>` + orphaned detached powershell** (2026-07-07). DeepSeek emitted a `<tool_use>` with swapped `</tool_use>`/`</arguments>` closing tags **and** invalid JSON (unescaped quotes around a Windows path). `parse_tool_calls` silently dropped it (log-only warn), `strip_tool_calls` erased it → empty turn → `AgentDone` with nothing shown/run; every later turn repeated → "frozen". The same unbalanced quote hung `powershell -Command` (unterminated string, `stdin=null`, `DETACHED_PROCESS`), and detached jobs are only reaped on graceful exit → a force-close orphaned 6 of them. **Fix:** (1) `parse_tool_calls_with_errors` + tolerant `extract_arguments` (swapped tags, dropped `<arguments>`, `{"name","arguments"}` shape) — broken JSON becomes a diagnostic, never a guessed call; (2) runner feeds the parse error back and retries (`MAX_MALFORMED_TOOL_RETRIES=2`) instead of ending silently; (3) Windows Job Object with `KILL_ON_JOB_CLOSE` binds every background/PTY child so a force-close kills the tree. `→ src/agent/tool_parser.rs`, `src/agent/runner.rs`, `src/tools/background/types.rs` (`win_job`) + `spawn.rs`. Write-up: `JOURNAL/2026-07-07.md` (pt.2).

- ✅ `~~ONNX embedder OOM on low-RAM hosts: `bad allocation` / `BFCArena Failed to allocate ~1.2 GB` on `/encoder/layer.0/attention/self/Add`~~` — `Embedder::embed(_, None)` let fastembed use its default batch of **256**; peak attention memory ≈ `batch × 12 heads × 512² × 4B` ≈ 3.2 GB, which OOMs on thin machines (surfaced during `backfill_history`, the heaviest embed pass). seq is already truncated to 512 and history already chunked, so batch was the only unbounded lever. Fixed 2026-07-06: new `[semantic] rag_limit` (`auto`/`off`/`<N>`) + `/rag-limit` command; **auto** sizes the batch from total RAM (`util::total_ram_bytes()`, per-platform, falls back to batch 8 when RAM can't be probed). e5-small is non-quantized so an arbitrary cap is safe. `→ src/semantic/embedder.rs`, `src/config/mod.rs`, `src/util.rs`, `src/commands/defs/rag_limit.rs`. Full write-up: `JOURNAL/2026-07-06.md` (pt.2).

- ✅ `~~Long chat lines (e.g. a markdown table) climb a few columns onto the right stats panel~~` — two compounding bugs: (1) `tui/render/mod.rs` reserved the panel column as `34.min(width/4)` while `widgets::panel` always draws a fixed 34, so on terminals < ~136 cols the chat `Rect` overlapped the panel and wrapped its text too wide; (2) the panel painted its background via `Block`/`Paragraph` *styles*, which only recolor cells and don't clear the glyphs already there, so overlapping chat text bled through the panel's blank/indented cells. Fixed 2026-07-06 (Session 5): `panel::panel_width()` = exact drawn width, reserved by the layout with a 1-col separator gap; `Clear` widget over `panel_area` before the bg fill; panel auto-collapses to the plain layout on very narrow terminals. `→ src/tui/render/mod.rs`, `src/tui/widgets/panel.rs`

- ✅ `~~Loading an existing local session (/sessions → /load) and sending a message silently created a brand-new DeepSeek remote session, even though the old one was still live~~` — root cause: the DeepSeek-side `SessionState` (`session_id`/`parent_message_id`) was never persisted anywhere, and `handle_load_session` unconditionally called `provider.reset()` on every local-session load, discarding any in-memory identity too. So *any* app restart, or any `/load`, always started a fresh remote `chat_session` with no way to tell. Fixed 2026-07-04: `Session` now persists `provider_session_id`/`provider_parent_message_id`/`broken`; `LLMProvider` gained `session_identity`/`session_is_alive`/`adopt_session`; `/load` checks aliveness off-loop and either resumes the same remote thread (`adopt_session`) or marks the session `broken` (yellow ⚠ in `/sessions`, cleared again once a fresh remote link is confirmed) and replays full local history as one prompt on the next message. Side-fix: `auto_save_session` previously wiped a session's `tag` back to `None` on every turn after a `/load`/`/import` (only ever wrote `tag: None` regardless of the file's actual tag) — now mirrored on `Conversation` and round-tripped correctly. Full flow in `reference/CONFIG.md` → "Remote session resume". `→ src/session.rs`, `src/provider/mod.rs`, `src/provider/deepseek/mod.rs`, `src/app/mod.rs`
- ✅ `~~PowerShell commands with genuinely empty output (e.g. a filter matching nothing) were reported to the agent as "[powershell exited successfully but returned no stdout/stderr; output capture likely failed]" — a ToolResult::error~~` — the 2026-07-03 evening pass added this as a speculative capture-failure guard, but exit-0 + empty stdout/stderr is a normal, common outcome (no matches, silent success like `Set-Content`), not evidence of a bug. The agent read the `is_error` flag and treated a healthy zero-result command as broken. Fixed 2026-07-04: same branch now returns `ToolResult::success("(command completed successfully with no output)")`; the `shell.foreground.empty_success` debug-log event is unchanged for diagnostics if a real capture bug ever surfaces. `→ src/tools/shell.rs`
- ✅ `~~Every turn ends with "stream ended early…" warning; final answers abort as AgentError~~` — the strict-stop pass wrongly assumed DeepSeek ends its SSE with `data: [DONE]`; the live protocol just closes the connection (0 `[DONE]` in a full session log). Clean EOF is now a normal stop in both `complete`/`complete_stream`; explicit finish signals (`[DONE]` ±space, `FINISHED` status patch, terminal BATCH) recognized in `process_stream_line`; unrecognized SSE lines now logged (`completion.*.skipped`). Fixed 2026-07-03 evening — see `JOURNAL/2026-07-03.md`. Watch item: one zero-content stream (metadata only, instant close) remains unexplained; skipped-line logging will identify it if it recurs.

- ✅ **2026-07-03 follow-ups** — ephemeral remote-session leak fixed: `LLMProvider::discard_remote_session` wired into sidechat/sub-agent finalize, stop, foreground `task` forks, and (bounded, 3s) app exit; user-facing `/delete [id]` + `/delete-local [id]` added (shared multi-select picker, All/Local/Remote filter = deletion scope, confirm step, background remote fetch/delete via `RemoteSessionsListed`/`SessionsDeleted` events); `deepseek.rs` split into `deepseek/{mod,http,session,stream,endpoints}.rs`; deps bumped (reqwest 0.13, ratatui 0.30, crossterm 0.29, pulldown-cmark 0.13, toml 1.x); PoW wasm embedded via `include_bytes!`.

- ✅ **2026-07-02 refactor session** — see `reference/AUDIT-2026-07-02.md` for full detail; `JOURNAL/2026-07-02.md` for the session narrative. One line per subsystem:
  - **Streaming**: `complete_stream` now spawned as a task so the 120s idle guard races the live network call; reqwest client got `connect_timeout(10s)`/`read_timeout(120s)`; stray `gzip` header removed; `SseLineBuffer` rewritten byte-based with a 4MiB cap (was unbounded + O(n²)).
  - **MCP**: lock-free `client_for` handles replace holding `MCPManager`'s mutex across network `.await`s; stdio stderr now drained; JSON-RPC `id` correlation replaces order-assumed matching; `Transport::close()` now has real call sites (toggle/remove/reload/app-exit `shutdown_all`); `connect_all` now concurrent; honest connection-failure status; `persist_config` no longer copies foreign servers' secrets.
  - **GOAL**: evaluator moved off the event loop via `spawn_goal_evaluation` → `GoalEvaluationDone`; dead duplicate inline path and unused `GoalCycleFinished` variant removed; stale-verdict guard added.
  - **Commands/ACP**: `/goal` leading-slash registration bug fixed (+ round-trip test); `--acp` nested-runtime panic fixed with `block_in_place`.
  - **Render**: `run_loop` drains ≤256 events then renders once behind a dirty flag (idle = zero draws); per-message markdown/syntect cache in `tui/widgets/chat.rs`; token-estimate caching.
  - **Interaction/approval**: single-slot approval waiter replaced by a `PendingInteraction` queue; Esc/Ctrl+C now cancels wedged turns; `whitelist::persist_approval` makes "always allow" survive restarts.
  - **Conversation model**: `ChatMessage.ui_only` splits model-visible history from UI chrome; unified reducer methods (`begin_assistant_message`/`append_chunk`/`discard_empty_assistant`/`finish_turn`) replace the diverged focused/background duplicate logic; `send_to_agent` → `send_focused_turn(Option<ChatMessage>)` whose argument now actually reaches the model.
  - **Shell/background**: foreground shell got a 300s timeout + 1MiB cap + kill_on_drop + tree-kill; PowerShell UTF-8 prefix fixes cp866 mojibake; background readers survive non-UTF8; Unix process-group kill; async `force_kill_pid`; overflow flag no longer sticky.
  - **Persistence**: `util::atomic_write` wired into config/session/whitelist/mcp-config saves (was unused, zero call sites); session version checked on load; config file 0600 on Unix.
  - **Misc**: `/help` generated from the command registry; `@file` mention range clamp; skills frontmatter keeps repeated keys; markdown bold/italic/strikethrough now actually styled; MCP panel gap underflow fixed; scroll row-count uses ratatui's real line-count; stats-panel/landing-session disk reads cached; retry backoff made `saturating`.
  - **Dead code removed**: `parse_sse_event`/`ParsedSSEEvent` family, ~12 dead structs/consts in `provider/types.rs`, `AppState.error`, `GoalCycleFinished`, `CommandResult::NeedsAgent`, unused theme fields, `BackgroundHandle.id`, `SpawnOutcome.status/interactive`, `API_BASE`.
  - **Result**: clippy ~220→0 warnings, tests 84→189, CI added (`.github/workflows/ci.yml`).
- ✅ `~~Messages "evaporate" — appear in the TUI but never reach DeepSeek / the web view~~` — `parent_message_id` desync forked the session onto an invisible branch (triggered by interrupted/errored streams). Fixed by incremental persist + flush-on-error (`deepseek.rs`, commit `183712e`); structurally prevented by per-conversation `fork()` isolation.
- ✅ `~~GOAL pipeline wedges on empty prompt/goal, interrupt+new message~~` — overhaul `c0d4280`.
- ✅ `~~GOAL cycle has no max-iteration cap~~` — was fixed by the goal overhaul; BUGS.md had gone stale. `MAX_GOAL_ITERATIONS = 10` (`src/app/goal.rs`); `apply_verdict` gives up at the cap (test `gives_up_at_iteration_cap`).
- `~~No .gitignore entry for .memories/JOURNAL/~~` — JOURNAL is intentionally tracked in git.
