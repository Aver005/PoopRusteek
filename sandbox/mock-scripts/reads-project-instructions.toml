# Проверяет, что AGENTS.md рабочей папки доезжает до системного промпта.
#
# Ответ здесь скриптованный, поэтому сценарий доказывает не «модель послушалась»,
# а факт доставки: его проверяет `[[expect.trace]]` по полю `instructions`
# записи `system_prompt.assembled`. Проверять послушание на моке бессмысленно —
# мок не читает промпт.

[[reply]]
content = "pomidor"
