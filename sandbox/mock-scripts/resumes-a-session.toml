# Продолжение сохранённой сессии: `--resume` поднимает историю прошлого
# запуска.
#
# Ответ скриптованный, потому что мок не читает промпт — доказывает сценарий
# не «модель вспомнила», а факт доставки: `[[expect.trace]]` смотрит на
# `harness.resume` и на `history_messages` записи `harness.turn.started`.

[[reply]]
content = "The magic word is pomidor."
